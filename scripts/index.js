const options = {
    weekday: "short",
    day:"numeric",
    month:"long",
    year:"numeric,"
};

const nyear = new Date();
document.getElementById(
    "copyyear"
).textContent = &copy; $(nyear.getFullYear()}| Lora Chisholm | Utah

const time = new Date(document.lastModified);

document.getElementById("copyright').textContent = 'Last updated: $(time.getDate()}';
